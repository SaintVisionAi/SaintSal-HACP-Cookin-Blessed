// Oversight engine placeholder. Connect to your learning/indexing logic.

export function analyzeImpact(changeSummary: string) {
  return {
    risk: 'low',
    notes: `Auto-assessed change: ${changeSummary.slice(0, 160)}...`
  };
}
